/**
  * NAME:
  * DATE:
  * FILE:
  * COMMENTS:
  */

public class LargestNnumbers extends eecs.Gui
{
   public static void main(String[] args)
   {
      // declare variables
      String raptor_prompt_variable_zzyz = null;
      ?? current = ??;
      ?? count = ??;
      ?? largest = ??;
      ?? n = ??;
      
      raptor_prompt_variable_zzyz = "enter the value of N";
      n = get???(raptor_prompt_variable_zzyz);
      count = 0;
      largest = 0;
      while	(1)
      {
         raptor_prompt_variable_zzyz = "Enter the number";
         current = get???(raptor_prompt_variable_zzyz);
         if (largest < current)
         {
            largest = current;
         }
         else
         {
         }
         count = count + 1;
         if (count >= n)	break;
      }
      printLine("largest number is" + largest);
   } // close main
} // close LargestNnumbers
