#include<stdio.h>
int main(){
    float b,h;
    float area;
    printf("enter the base of the triangle");
    scanf("%f", &b);
    scanf("%f", &h);
    area = 0.5*b*h;
    printf("area of the circle is %f",area);
    return 0;
}

